<Window x:Class="ParallelSystemPlugin.UI.FilterItemsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Filter Items"
        Width="720"
        Height="720"
        MinWidth="560"
        MinHeight="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        FontFamily="Segoe UI Variable, Segoe UI, Segoe UI Historic, Arial, Helvetica, sans-serif"
        FontSize="14"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
        ShowInTaskbar="False"
        Topmost="False">

    <Window.Resources>
        <SolidColorBrush x:Key="InfoBrush" Color="#FF2563EB"/>

        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid x:Name="Root" RenderTransformOrigin="0.5,0.5">
                            <Grid.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Grid.RenderTransform>
                            <Border Background="{TemplateBinding Background}" CornerRadius="12"/>
                            <Border x:Name="Hover" CornerRadius="12" Background="White" Opacity="0"/>
                            <Border x:Name="Press" CornerRadius="12" Background="Black" Opacity="0"/>
                            <ContentPresenter Margin="12,0"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Hover" Property="Opacity" Value="0.12"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Press" Property="Opacity" Value="0.16"/>
                                <Setter TargetName="Root" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.985" ScaleY="0.985"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="12,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#0F000000"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1A000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Expander">
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True" Margin="0,0,0,10">
            <Grid Width="28" Height="28" Margin="0,0,10,0">
                <Path Fill="{StaticResource InfoBrush}"
                      Stroke="{StaticResource InfoBrush}"
                      StrokeThickness="1.6"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"
                      Data="M3,5 L10,5 L12,8 L21,8 L21,20 L3,20 Z M7,12 L17,12 M7,16 L14,16"/>
            </Grid>

            <StackPanel>
                <TextBlock Text="Filter Items"
                           FontSize="17"
                           FontWeight="SemiBold"/>
                <TextBlock x:Name="PART_ViewName"
                           Margin="0,2,0,0"
                           Opacity="0.72"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1"
                Margin="0,0,0,12"
                Padding="12"
                CornerRadius="10"
                Background="#0A000000"
                BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Checked = visible. Unchecked = temporarily hidden in this view. Validate an item against your report, then uncheck it. When every validated item is unchecked, anything still visible in Revit needs investigation."
                           TextWrapping="Wrap"/>
                <TextBlock x:Name="PART_Summary"
                           Margin="0,8,0,0"
                           FontWeight="SemiBold"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Groups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Expander IsExpanded="True">
                                <Expander.Header>
                                    <DockPanel LastChildFill="True" Margin="2,4">
                                        <TextBlock Text="{Binding Name}"
                                                   FontWeight="SemiBold"
                                                   FontSize="14.5"/>
                                        <TextBlock Text="{Binding Quantity, StringFormat='QTY: {0}'}"
                                                   Margin="10,0,4,0"
                                                   HorizontalAlignment="Right"
                                                   Opacity="0.72"/>
                                    </DockPanel>
                                </Expander.Header>

                                <ItemsControl ItemsSource="{Binding Items}" Margin="24,2,4,8">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="8,3"
                                                    Margin="0,0,0,3"
                                                    CornerRadius="7"
                                                    Background="#05000000">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <CheckBox Grid.Column="0"
                                                              IsChecked="{Binding IsVisible, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                              Content="{Binding DisplayName}"
                                                              ToolTip="Uncheck to temporarily hide every matching instance in the active view."/>

                                                    <Border Grid.Column="1"
                                                            Margin="12,0,0,0"
                                                            Padding="9,3"
                                                            CornerRadius="10"
                                                            Background="#10000000"
                                                            VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Quantity, StringFormat='QTY {0}'}"
                                                                   FontWeight="SemiBold"/>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Expander>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Content="Check All"
                        Style="{StaticResource SecondaryButton}"
                        Click="OnCheckAllClick"
                        Margin="0,0,8,0"/>
                <Button Content="Uncheck All"
                        Style="{StaticResource SecondaryButton}"
                        Click="OnUncheckAllClick"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Content="Close"
                    Style="{StaticResource AccentButton}"
                    Click="OnCloseClick"/>
        </Grid>
    </Grid>
</Window>
